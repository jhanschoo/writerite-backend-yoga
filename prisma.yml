endpoint: http://localhost:4466/writerite/dev
datamodel: datamodel.prisma

generate:
  - generator: typescript-client
    output: generated/prisma-client/
  - generator: graphql-schema
    output: generated/
hooks:
  post-deploy:
    - prisma reset --force
    - prisma generate
    - graphql get-schema --project prisma
